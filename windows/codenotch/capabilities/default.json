{
  "identifier": "default",
  "description": "Codenotch notch & settings windows: allow core APIs (event.listen is required for state broadcasts, and the settings window needs them too)",
  "windows": ["notch", "settings"],
  "permissions": ["core:default"]
}
