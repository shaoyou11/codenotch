# 个人缩小版

本地目录：`~/Documents/Xcode/codenotch`。

## 改动

- 圆环、图标、文字、黑色背景、设置手柄和提示卡片的统一尺寸系数为上游的 50%。宽高减半，不是面积减半。
- 完整保留上游 Option（⌥）＋拖动：沿当前屏幕边缘移动，松开后保存位置。左右边缘上下移动，上下边缘左右移动；始终贴在所选边缘。
- 定制版停用官方自动更新，包括之前官方版保存的开启状态，避免悄悄覆盖定制版。设置页面显示说明。
- 使用临时本地签名，自用无需付费开发者账号。重新编译后可能需要重新允许钥匙串访问。

## 本机构建

```sh
bash Scripts/custom-build.sh
```

成品位于 `build/custom/Codenotch-50percent.zip`。解压后将应用放进「应用程序」。替换前退出旧版，并先将旧版移到备份目录。构建脚本不安装应用、不删除文件。

## 跟进上游

`main` 保留为上游基线，个人修改集中在 `custom/compact-draggable` 分支。
`origin` 是自己的 fork，`upstream` 是作者仓库。

```sh
bash Scripts/custom-sync.sh
bash Scripts/custom-build.sh
git push origin HEAD
```

有冲突时脚本停止，不自动覆盖。合并前先查看上游更新说明：目前拖动已有上游实现；如果以后上游支持所需的 50% 缩放，就无需继续维护此分支。

## 不想本地编译

推送定制分支后，GitHub Actions 的「Build compact app」自动编译，可从该次运行的 Artifacts 下载 `Codenotch-50percent`。也可以在 Actions 手动选择定制分支运行。首次使用 fork 的 Actions 可能需要在网页启用工作流。产物保留 30 天，到期可以重新运行。

这会省去本机编译，但同步上游仍需合并、检查冲突；没有自动无条件覆盖或定时任务。

## 换回官方版

退出定制版，把它移到备份位置，再安装作者发布的官方版；在官方版设置中确认开启自动更新。无需删除账号资料或运行任何清理脚本。保留 fork 和本地源码即可随时回退。
